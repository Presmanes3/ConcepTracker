[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "conceptracker"
version = "0.1.0"
dependencies = [
    "langgraph",
    "pydantic",
    "langchain"
]

[tool.setuptools.packages.find]
where = ["."]